title: Request de veículo
description: Dados para criar ou atualizar um veículo
type: object
required: [ licensePlate, brand, model, year, color ]
properties:
  licensePlate: { type: string }
  brand: { type: string }
  model: { type: string }
  year: { type: integer }
  color: { type: string }
