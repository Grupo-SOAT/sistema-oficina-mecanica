title: Dados do serviço
description: Informações de um serviço oferecido
type: object
required: [ serviceOrderId, catalogService, price ]
properties:
  serviceOrderId: { type: integer, format: int64 }
  catalogServiceId: { type: integer, format: int64 }
  price: { type: number, format: decimal }
  neededSupplies: { type: array, items: { $ref: './NeededSupplyData.yaml' } }
