title: Ação a ser processada sobre uma ordem de Serviço
type: object
required: [ action ]
properties:
  action: { $ref: './ServiceOrderAction.yaml' }
  relatedServiceId: { type: integer, format: int64 }
